<!-- BEGIN_AUTOGENERATED -->

# ForwardedArgs Node Formatting

Represents a `...` operator that contains forwarded argument (see `ForwardArg`)

<!-- END_AUTOGENERATED -->

## Formats

Before:

```ruby
def perform(...)
  another_method(...)
end
```

After:

```ruby
def perform(...)
  another_method(...)
end
```
