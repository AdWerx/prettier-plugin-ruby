# Defs Formatting

## Formats

Before:

```ruby
def self.name
  "foo"
end
```

After:

```ruby
def self.name
  "foo"
end
```

## Formats an endless method definition

Before:

```ruby
def self.m() = 42
```

After:

```ruby
def self.m() = 42
```
